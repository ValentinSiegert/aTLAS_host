import random


def specificity(ID, entity, tag):
    pass
    # file_name = ID + ".txt"
    # log_path = Logging.LOG_PATH / file_name
    # fo = open(log_path.absolute(), "r+")
    # logfile = fo.read()
    # filesize = len(logfile)
    # fo.seek(0)
    # expresult = 0
    # trustvalue = 0
    # while fo.tell() < filesize:
    #     timelog_line = fo.readline()
    #     if timelog_line[56:58] == tag:
    #         expresult = random.random()
    #         trustvalue = format(expresult, '.2f')
    # fo.close()
    # return trustvalue